name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# v1.5.2: action tags pinned to majors that declare Node 24 natively in their
# action.yml (checkout@v5, setup-node@v5, pnpm/action-setup@v5,
# softprops/action-gh-release@v3, upload-artifact@v6). Removed v1.5.2: the
# FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 env override is no longer needed.

jobs:
  backend:
    name: Backend (Node.js)
    runs-on: macos-14
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v5
        with:
          version: 9
      - uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: backend/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm run lint
      - name: Type-check (allowlist for rag + logger)
        run: pnpm run lint:types
      - run: pnpm test
      - name: Performance budgets (stubbed LLM)
        run: pnpm run perf
      - name: Audit production dependencies
        run: pnpm audit --prod --audit-level=high

  swift-packages:
    name: Swift packages
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v5
      - name: Select Xcode 15.4
        run: sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer
      - name: Test LessonKit
        working-directory: app/Packages/LessonKit
        run: swift test
      - name: Test BackendClient
        working-directory: app/Packages/BackendClient
        run: swift test
      - name: Build app shell
        working-directory: app/GramartEnglish
        run: swift build
      - name: Test app (unit + perf benches)
        working-directory: app/GramartEnglish
        # Includes Tests/Perf which holds the mode-card render bench (T054)
        # and the audio first-token bench (T055). Audio bench is gated on the
        # GRAMART_PERF_AUDIO env var so CI doesn't try to actually play sound.
        run: swift test
